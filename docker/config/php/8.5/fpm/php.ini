; Minimal php.ini — delegates defaults to the PHP package baseline.
; All application-specific settings are in conf.d/99-symfony.ini.
;
; Only settings that neither the package default nor 99-symfony.ini cover
; are set here.

[PHP]
; Hide the PHP version from HTTP response headers (hardened default).
expose_php = Off

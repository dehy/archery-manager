{% extends 'public.html.twig' %}

{% block title %}Pré-inscription Renouvellement{% endblock %}

{% block body %}
    <div class="row my-3">
        <div class="col-12">
            <img src="{{ asset('build/logo-archers-de-guyenne.svg') }}" alt="" class="float-start me-3" style="height: 100px" />
            <h1>Les Archers de Bordeaux Guyenne</h1>
            <h2>Pré-inscription Renouvellement <small class="text-muted">Saison 2023 (septembre 2022)</small></h2>
        </div>
    </div>

    {% if error %}
        <div class="row">
            <div class="col-12">
                <div class="alert alert-danger">
                    Ce formulaire est destiné aux archers du club déjà inscrit la saison précédente. Pour une nouvelle
                    adhésion, utiliser ce formulaire :
                    <a href="{{ path('app_pre_registration') }}">Pré-inscription nouvel archer</a>
                </div>
            </div>
        </div>
    {% endif %}

    <div class="row">
        <div class="col-md-5">
            <div class="alert alert-info">
                <p>
                    Afin de préparer la prochaine saison qui débutera en septembre 2022,
                    si vous souhaitez renouveler votre adhésion aux Archers de Bordeaux Guyenne,
                    merci de remplir le formulaire suivant.</p>
            </div>
        </div>

        <div class="col-md-7">
            {% form_theme form 'bootstrap_5_layout.html.twig' %}
            {{ form_errors(form) }}
            {{ form_start(form) }}
            <fieldset>
                <legend>Informations sur le pratiquant</legend>
                <div class="row mb-3">
                    <div class="col">
                        {{ form_label(form.lastname) }}
                        {{ form_widget(form.lastname) }}
                    </div>
                    <div class="col">
                        {{ form_label(form.firstname) }}
                        {{ form_widget(form.firstname) }}
                    </div>
                </div>
                {{ form_row(form.licenseNumber) }}
                {{ form_row(form.licenseType) }}
            </fieldset>
            {{ form_row(form.comment) }}
            {{ form_rest(form) }}
        </div>
    </div>
{% endblock %}
